generateFiles:
  handler: src/functions/generateFiles.handler
  timeout: 600 
  memorySize: 512
  environment:
    bucketName: ${self:custom.bucketFeeds}
  events:    
    - http:
        path: products/run-update-xml
        method: get
        authorizer: verifyToken
        cors: true 
      
        